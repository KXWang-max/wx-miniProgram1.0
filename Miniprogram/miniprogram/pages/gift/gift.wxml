<view class="gift">
    <block class="list" wx:for="{{giftList}}" wx:key="index">
        <view class="giftItem">
          <image class="img" src="{{item.url}}"></image>
          <view class="text">{{item.text}}</view>
        </view>
    </block>
    <view class="h">到底啦!到分类中查看更多~</view>
</view>